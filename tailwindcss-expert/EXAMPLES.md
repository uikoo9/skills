# Tailwind CSS Expert Skill - 使用示例

安装 skill 后，以下是一些实际使用示例。

## 示例 1: 创建响应式按钮

**提问:**
```
用 Tailwind CSS 创建一个主要按钮和次要按钮
```

**期望回答包含:**
- 主要按钮的完整 HTML（带背景色、hover 效果、padding、圆角）
- 次要按钮变体
- 禁用状态
- 不同尺寸的按钮

---

## 示例 2: 响应式卡片组件

**提问:**
```
展示一个响应式的产品卡片，包含图片、标题、描述和价格
```

**期望回答包含:**
- 完整的卡片 HTML 结构
- 图片容器和样式
- 文本排版
- 阴影和圆角
- 响应式调整（mobile 到 desktop）

---

## 示例 3: Flexbox 布局

**提问:**
```
如何用 Tailwind Flexbox 创建一个居中的登录表单？
```

**期望回答包含:**
- 使用 `flex items-center justify-center` 居中
- `min-h-screen` 全屏高度
- 表单容器样式
- 输入框和按钮样式
- 完整可用的代码

---

## 示例 4: Grid 响应式布局

**提问:**
```
创建一个响应式的网格布局：移动端 1 列，平板 2 列，桌面 3 列
```

**期望回答包含:**
- `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3`
- Gap 间距
- 网格项的样式
- 完整示例

---

## 示例 5: 深色模式实现

**提问:**
```
如何实现深色模式切换？展示完整的示例
```

**期望回答包含:**
- HTML 结构（包含切换按钮）
- 使用 `dark:` 变体的样式
- JavaScript 切换逻辑
- localStorage 保存偏好
- 系统主题检测

---

## 示例 6: 导航栏

**提问:**
```
创建一个响应式导航栏，移动端有汉堡菜单
```

**期望回答包含:**
- 导航栏容器样式
- Logo 和菜单项
- 移动端隐藏/显示逻辑
- 汉堡图标
- Sticky 定位

---

## 示例 7: 渐变背景

**提问:**
```
如何创建一个从蓝色到紫色的渐变背景？
```

**期望回答包含:**
- `bg-gradient-to-r from-blue-500 to-purple-600`
- 不同方向的渐变
- 多色停靠点
- 自定义渐变位置

---

## 示例 8: 表单验证样式

**提问:**
```
展示一个带验证状态的表单输入框
```

**期望回答包含:**
- 基础输入框样式
- Focus 状态
- Invalid 状态（红色边框）
- 成功状态（绿色边框）
- 错误消息显示

---

## 示例 9: 模态框

**提问:**
```
用 Tailwind 创建一个居中的模态框
```

**期望回答包含:**
- Fixed 遮罩层
- 居中的模态框容器
- 关闭按钮
- 头部、内容、底部区域
- 响应式设计

---

## 示例 10: Hover 效果卡片

**提问:**
```
创建一个鼠标悬停时有动画效果的卡片
```

**期望回答包含:**
- 基础卡片样式
- `hover:shadow-xl` 阴影过渡
- `hover:scale-105` 缩放效果
- `transition duration-300` 过渡动画
- `transform` 变换

---

## 示例 11: Badge 和 Tag

**提问:**
```
展示各种样式的 badge（徽章）
```

**期望回答包含:**
- 基础 badge 样式
- 不同颜色（success, warning, error）
- 带圆点的 badge
- Pill 形状
- 不同尺寸

---

## 示例 12: 文本截断

**提问:**
```
如何截断过长的文本并显示省略号？
```

**期望回答包含:**
- 单行截断：`truncate`
- 多行截断：`line-clamp-3`
- 文本换行控制
- 实际使用场景

---

## 示例 13: 容器查询（V4）

**提问:**
```
什么是容器查询？如何使用？
```

**期望回答包含:**
- `@container` 基础用法
- 与响应式断点的区别
- 命名容器
- 实际应用场景
- V4 新特性说明

---

## 示例 14: Group 和 Peer 变体

**提问:**
```
解释 group 和 peer 变体的用法，并给出示例
```

**期望回答包含:**
- Group hover 示例
- 命名 group
- Peer checked 示例
- 实际使用场景
- 完整代码

---

## 示例 15: Alert 提示框

**提问:**
```
创建成功、警告、错误三种提示框
```

**期望回答包含:**
- 三种不同颜色的 alert
- 带图标的版本
- 左侧彩色边框
- 关闭按钮
- 响应式设计

---

## 示例 16: Loading 状态

**提问:**
```
如何用 Tailwind 创建一个加载动画？
```

**期望回答包含:**
- Spinner 动画
- `animate-spin` 工具类
- Pulse 动画
- Skeleton 加载状态
- 进度条

---

## 示例 17: Arbitrary Values

**提问:**
```
什么是任意值？如何使用？
```

**期望回答包含:**
- `w-[347px]` 语法
- 任意颜色 `bg-[#1da1f2]`
- 任意属性 `[mask-type:luminance]`
- 计算值 `w-[calc(100%-2rem)]`
- 使用场景

---

## 示例 18: 自定义配置

**提问:**
```
如何在 Tailwind v4 中自定义颜色和间距？
```

**期望回答包含:**
- `@theme` 语法
- 自定义颜色变量
- 自定义间距值
- 自定义断点
- CSS 配置示例

---

## 示例 19: 可访问性

**提问:**
```
展示 Tailwind 中可访问性的最佳实践
```

**期望回答包含:**
- Focus states
- Screen reader 工具类
- Skip to content 链接
- ARIA 属性
- 键盘导航

---

## 示例 20: 完整的登录页面

**提问:**
```
用 Tailwind CSS 创建一个完整的登录页面
```

**期望回答包含:**
- 全屏居中布局
- 登录表单卡片
- 输入框样式（email, password）
- 记住我复选框
- 提交按钮
- 忘记密码链接
- 响应式设计
- 完整可用的 HTML

---

## 验证 Skill 是否工作

安装后，尝试提问上述任何问题。如果 skill 正常工作，你应该会收到：

1. ✅ **完整的 HTML 代码** - 可以直接复制使用
2. ✅ **准确的类名** - 如 `bg-blue-500` 而非 `bg-blue`
3. ✅ **详细的解释** - 说明每个类的作用
4. ✅ **最佳实践** - 包含响应式、可访问性建议
5. ✅ **现代语法** - 使用最新的 Tailwind v4 特性

## 进阶使用

### 组合查询
```
创建一个深色模式的响应式卡片网格，每张卡片有 hover 效果
```

### 特定需求
```
我需要一个固定在底部的通知栏，移动端占满宽度，桌面端居中最大宽度为 1280px
```

### 问题排查
```
为什么我的 hover:bg-blue-700 不工作？
```

### 性能优化
```
Tailwind CSS 的性能优化建议有哪些？
```

---

**提示**：可以随时向 Claude Code 提出任何 Tailwind CSS 相关的问题，skill 会提供准确的答案和完整的代码示例！
